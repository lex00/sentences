node_modules/
dist/
*.local
.DS_Store

# The 72 MB neural weights are a build artifact (regenerate via parser-export/, or fetch the
# published Release asset at runtime). The small tokenizer/vocab configs ARE committed so a
# static deploy can serve them from /models/.
public/models/*.onnx
