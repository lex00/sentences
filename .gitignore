node_modules/
dist/
*.local
.DS_Store

# The 72 MB neural weights stay OUT of git. They live as a GitHub Release asset; the deploy
# workflow downloads them at build time (server-side, no CORS) and ships them in dist/models/ so
# Pages serves them same-origin. Regenerate the weights via parser-export/. Small configs commit.
public/models/*.onnx
