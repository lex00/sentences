{
  "name": "sentences",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Effects-rich, interactive Reed-Kellogg sentence-diagram renderer",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
